name: CI flow for Backend

# BackendのPR時にテストを実行し、結果をSlackへ通知するワークフロー
on:
  # develop向けのPR作成/更新/再オープン時に起動
  pull_request:
    branches:
      - develop
    types: [opened, synchronize, reopened]

env:
  # JavaScriptアクションをNode.js 24で実行
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  # Slack通知先Webhook（GitHub Secretsから参照）
  SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}

jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
    # ソースコードをチェックアウト
    - uses: actions/checkout@v4
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
       node-version: 24

    # テスト実行（リポジトリルートで依存関係を導入して実行）
    - name: Unit Test
      run: cd ${{ github.workspace }}
    - run: npm install && npm run test

    # 成功/失敗/キャンセル時のSlack通知
    - name: Success
      uses: rtCamp/action-slack-notify@v2.0.2
      if: success() && env.SLACK_WEBHOOK != ''
      env:
        SLACK_TITLE: CI flow for Backend / Success
        SLACK_COLOR: good
        SLACK_MESSAGE: Message for Success

    - name: Failure
      uses: rtCamp/action-slack-notify@v2.0.2
      if: failure() && env.SLACK_WEBHOOK != ''
      env:
        SLACK_TITLE: CI flow for Backend / Failure
        SLACK_COLOR: danger
        SLACK_MESSAGE: Message for Failure

    - name: Cancel
      uses: rtCamp/action-slack-notify@v2.0.2
      if: cancelled() && env.SLACK_WEBHOOK != ''
      env:
        SLACK_TITLE: CI flow for Backend / Cancel
        SLACK_COLOR: danger
        SLACK_MESSAGE: Message for Cancel
